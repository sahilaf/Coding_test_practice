{
  "name": "Vite + Spring Boot Dev Environment",
  "image": "mcr.microsoft.com/devcontainers/java:21",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "18"
    },
    "ghcr.io/devcontainers/features/git:1": {}
  },

  // Optional: Install Maven if not included in the base image
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y maven",

  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "vscjava.vscode-maven",
        "github.copilot",
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint"
      ]
    }
  },

  "forwardPorts": [8080, 5173],

  "portsAttributes": {
    "8080": {
      "label": "Spring Boot Backend",
      "onAutoForward": "notify"
    },
    "5173": {
      "label": "Vite Frontend",
      "onAutoForward": "notify"
    }
  },

  "remoteUser": "vscode",

  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],

  "postAttachCommand": {
    "backend": "cd backend && ./mvnw spring-boot:run",
    "frontend": "cd frontend && npm install && npm run dev -- --host"
  }
}
